# NOCKO Proxy Agent — Implementation Plan (Phase 1 MVP)\n\nСоздание нового агента с нуля в `/Users/rakhat/Documents/webhosting/NOCKO MDM/proxy-agent/`.\nОсновано на `proxy_agent_tz.md`. Работает на Ubuntu 22.04/24.04, Python 3.11.\n\n---\n\n## Proposed Changes\n\n### New Module: `proxy-agent/`\n\n#### [NEW] `proxy-agent/requirements.txt`\nЗависимости:\n```\nfastapi>=0.110\nuvicorn>=0.29\nhttpx>=0.27\npaho-mqtt>=2.0\npuresnmp>=2.0\nsqlmodel>=0.0.16\naiofiles>=23.0\njinja2>=3.1\nalembic>=1.13\n```\n\n#### [NEW] `proxy-agent/config.json.example` — Local config (LOCAL ONLY fields)\n```json\n{\n  "listen_host": "0.0.0.0",\n  "listen_port": 8765,\n  "log_level": "INFO",\n  "mdm_url": "https://mdm.nocko.com",\n  "enrollment_token": ""\n}\n```\n\n#### [NEW] `proxy-agent/core/config.py`\nЗагрузка конфига из `config.json`, разделение LOCAL / SERVER-MANAGED полей.\n\n#### [NEW] `proxy-agent/core/database.py`\nSQLite + SQLModel. Таблицы согласно TZ разделу 6.5:\n- `QueueItem` — offline buffer\n- `DeviceProfile` — загруженные профили\n- `Device` — устройства\n- `AgentConfig` — key-value конфиг\n- `Command` + `CommandResult`\n- `TrapArchive`\n- `InventoryCache`\n- `AuditLog`\n- `AgentCertMeta`\n\n#### [NEW] `proxy-agent/core/mqtt_client.py`\n`paho-mqtt` клиент:\n- Подключение к брокеру с client certificate\n- Авто-reconnect + offline buffer\n- Публикация в топики data plane\n- Подписка на `commands` и `config` топики\n- `agent_presence_heartbeat` каждые 60 сек (QoS 0)\n\n#### [NEW] `proxy-agent/core/bootstrap.py`\n`httpx` клиент:\n- `POST /api/v1/agent/register` с enrollment token\n- `GET /api/v1/agent/config` — получение конфига\n- `GET /api/v1/agent/items?profile_id=X` — получение items\n- Хранение client certificate после регистрации\n\n#### [NEW] `proxy-agent/core/queue.py`\nОфлайн-очередь:\n- Запись в `QueueItem` при сбое MQTT\n- FIFO replay для events\n- TTL 24ч для metrics, downsampling при backlog\n- `enqueue_timestamp` в каждой записи\n\n#### [NEW] `proxy-agent/collectors/snmp_poller.py`\nSNMP поллер (MVP Phase 1):\n- `puresnmp` async для SNMPv3 и v2c\n- Классы метрик: `fast`, `slow`, `inventory`, `lld`\n- Discard unchanged + `metric_keepalive`\n- Формирует payload envelope (Section 7 формат)\n\n#### [NEW] `proxy-agent/collectors/trap_receiver.py`\nUDP :162 trap listener:\n- `asyncio` UDP datagram protocol\n- Write-ahead в `TrapArchive`\n- Anti-storm: лимит traps/sec, агрегация флуда\n- Формирует events payload\n\n#### [NEW] `proxy-agent/console/app.py`\nLocal Web Console (FastAPI + Jinja2):\n- `GET /` — dashboard: статус агента, устройства\n- `GET /devices` — список устройств\n- `GET /config` — просмотр конфига\n- `POST /config` — редактирование LOCAL ONLY полей\n- `POST /profiles/upload` — загрузка Zabbix XML шаблона\n- `GET /logs` — просмотр логов\n\n#### [NEW] `proxy-agent/console/templates/` — Jinja2 HTML\nПростой UI без Node.js/Nginx.\n\n#### [NEW] `proxy-agent/watcher.py`\nWatcher-процесс: запускает агент, перезапускает при падении.\n\n#### [NEW] `proxy-agent/main.py`\nТочка входа: запуск всех компонентов через `asyncio`.\n\n#### [NEW] `proxy-agent/install.sh`\nBootstrap-скрипт:\n- Pre-flight check (Ubuntu, Python 3.11, disk)\n- `apt` зависимости\n- venv + pip\n- systemd service\n- Self-signed cert для Local UI (MVP)\n\n---\n\n## Verification Plan\n\n### Unit Tests\n```bash\ncd proxy-agent\npython -m pytest tests/ -v\n```\nТесты будут написаны вместе с кодом в `proxy-agent/tests/`:\n- `test_config.py` — загрузка конфига\n- `test_queue.py` — запись/replay очереди\n- `test_payload.py` — формирование envelope payload (Section 7)\n- `test_poller.py` — mock SNMP ответ → payload\n\n### Manual Smoke Test\n1. `cd proxy-agent && python main.py --dev` — запуск без MQTT\n2. Открыть `http://localhost:8765` → должен открыться Local UI\n3. Проверить раздел `/config` — конфиг загружен\n4. Проверить раздел `/devices` — список пустой (норма на старте)\n\n---\n\n## Структура директорий\n\n```\nproxy-agent/\n  main.py\n  watcher.py\n  install.sh\n  requirements.txt\n  config.json.example\n  core/\n    config.py\n    database.py\n    mqtt_client.py\n    bootstrap.py\n    queue.py\n    logger.py\n  collectors/\n    snmp_poller.py\n    trap_receiver.py\n  console/\n    app.py\n    templates/\n      base.html\n      dashboard.html\n      devices.html\n      config.html\n      logs.html\n  tests/\n    test_config.py\n    test_queue.py\n    test_payload.py\n    test_poller.py\n```\n
